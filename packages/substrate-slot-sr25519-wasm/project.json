{
  "name": "@novasamatech/substrate-slot-sr25519-wasm",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "cache": true,
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["npm run build:ts"],
        "parallel": false
      }
    }
  }
}
